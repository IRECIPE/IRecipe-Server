package umc.IRECIPE_Server.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import umc.IRECIPE_Server.domain.mapping.Refri;

public interface RefriRepository extends JpaRepository<Refri, Long> {
}
